apiVersion: v1
kind: Service
metadata:
  name: trino-service
  labels:
    instance: presto
    role: coordinator
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    instance: presto
    role: coordinator
